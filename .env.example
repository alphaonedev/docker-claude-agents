# =============================================================================
# Docker Claude Agents — Environment Configuration
# =============================================================================
#
# Setup:
#   cp .env.example .env
#   Edit .env and set your ANTHROPIC_API_KEY (required)
#
# =============================================================================

# ── Required ────────────────────────────────────────────────────────────────

# Your Anthropic API key (https://console.anthropic.com/settings/keys)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# ── Optional: Claude Code Version ──────────────────────────────────────────

# Pin a specific Claude Code version for reproducible builds.
# Set to "latest" for most recent version.
# CLAUDE_CODE_VERSION=latest

# ── Optional: MCP Services ─────────────────────────────────────────────────
# Only needed when using: docker-compose.mcp.yml

# GitHub Personal Access Token (https://github.com/settings/tokens)
# Required scopes: repo, read:org
# GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Brave Search API Key (https://brave.com/search/api/)
# BRAVE_API_KEY=BSA_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ── Optional: PostgreSQL (for MCP database access) ─────────────────────────

# POSTGRES_USER=claude_agent
# POSTGRES_PASSWORD=change_me_in_production
# POSTGRES_DB=agentdb

# ── Optional: LangGraph Deep Agents ───────────────────────────────────────
# Only needed when using: docker-compose.langgraph.yml

# OpenAI API Key (for LangGraph LLM calls)
# OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# LangSmith tracing (https://smith.langchain.com/)
# LANGCHAIN_API_KEY=lsv2_xxxxxxxxxxxxxxxxxxxxxxxxxxxx
# LANGCHAIN_TRACING_V2=true
# LANGCHAIN_PROJECT=docker-claude-agents

# LangGraph PostgreSQL (separate from MCP PostgreSQL)
# LANGGRAPH_POSTGRES_USER=langgraph
# LANGGRAPH_POSTGRES_PASSWORD=langgraph_secret
# LANGGRAPH_POSTGRES_DB=langgraph

# LangGraph API port (default: 8123)
# LANGGRAPH_PORT=8123

# ── Optional: Microsoft Agent Framework ───────────────────────────────────
# Only needed when using: docker-compose.msagent.yml

# Azure OpenAI (preferred for Microsoft Agent Framework)
# AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
# AZURE_OPENAI_API_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# AZURE_OPENAI_DEPLOYMENT=gpt-4o

# Kernel Memory embedding deployment
# KM_EMBEDDING_DEPLOYMENT=text-embedding-3-large

# Microsoft Agent Framework ports
# MSAGENT_GRPC_PORT=50051
# MSAGENT_HTTP_PORT=8200
