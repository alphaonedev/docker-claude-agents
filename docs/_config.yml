title: Docker Claude Agents
description: Multi-framework AI agent orchestration platform — Claude Code, LangGraph, Microsoft Agent Framework
baseurl: "/docker-claude-agents"
url: "https://alphaonedev.github.io"

theme: jekyll-theme-cayman

plugins:
  - jekyll-seo-tag

exclude:
  - Gemfile
  - Gemfile.lock

defaults:
  - scope:
      path: ""
    values:
      layout: default
